[pytest]
pythonpath = .
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)s] %(name)s: %(message)s
log_cli_date_format = %H:%M:%S
filterwarnings = ignore:urllib3 v2 only supports OpenSSL
reruns = 3
reruns_delay = 1
addopts = --rerun-except=no_retry
