{
    "server": "http://localhost:4723",
    "android": {
        "platform_name": "Android",
        "device_name": "${DEVICE_NAME}",
        "automation_name": "UiAutomator2",
        "expected_locale": "en",
        "device_pin": "${DEVICE_PIN}"
    },
    "recording": {
        "enabled": true
    },
    "updates": {
        "enabled": "${DEFAULT_UPDATES:-true}",
        "timeout": 900
    }
}
