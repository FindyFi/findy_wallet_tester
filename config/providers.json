{
    "issuers": {
        "authbound": {
            "type": "authbound",
            "base_url": "${ISSUER_AUTHBOUND_URL:-https://authbound.pensiondemo.findy.fi}",
            "credentials": {
                "credential_issuance": { "type": "issuance", "slug": "kael" }
            }
        },
        "hovi": {
            "base_url": "${ISSUER_HOVI_URL:-https://issuer.hovi.pensiondemo.findy.fi}",
            "credentials": {
                "credential_issuance": { "type": "issuance", "path": "pensioncredential.json" }
            }
        },
        "procivis": {
            "base_url": "${ISSUER_PROCIVIS_URL:-https://issuer.procivis.pensiondemo.findy.fi}",
            "credentials": {
                "credential_issuance": { "type": "issuance", "path": "pensioncredential.json" }
            }
        },
        "waltid": {
            "base_url": "${ISSUER_WALTID_URL:-https://issuer.waltid.pensiondemo.findy.fi}",
            "credentials": {
                "credential_issuance": { "type": "issuance", "path": "pensioncredential.json" }
            }
        },
        "paradym": {
            "base_url": "${ISSUER_PARADYM_URL:-https://issuer.paradym.pensiondemo.findy.fi}",
            "credentials": {
                "credential_issuance": { "type": "issuance", "path": "pensioncredential.json" }
            }
        },
        "sphereon": {
            "base_url": "${ISSUER_SPHEREON_URL:-https://issuer.sphereon.pensiondemo.findy.fi}",
            "credentials": {
                "credential_issuance": { "type": "issuance", "path": "pensioncredential.json" }
            }
        }
    },
    "verifiers": {
        "authbound": {
            "type": "authbound",
            "base_url": "${VERIFIER_AUTHBOUND_URL:-https://authbound.pensiondemo.findy.fi}",
            "credentials": {
                "pension_verification": { "type": "verification" }
            }
        },
        "hovi": {
            "base_url": "${VERIFIER_HOVI_URL:-https://verifier.hovi.pensiondemo.findy.fi}",
            "credentials": {
                "pension_verification": { "type": "verification", "path": "/" }
            }
        },
        "procivis": {
            "base_url": "${VERIFIER_PROCIVIS_URL:-https://verifier.procivis.pensiondemo.findy.fi}",
            "credentials": {
                "pension_verification": { "type": "verification", "path": "/" }
            }
        },
        "waltid": {
            "base_url": "${VERIFIER_WALTID_URL:-https://verifier.waltid.pensiondemo.findy.fi}",
            "credentials": {
                "pension_verification": { "type": "verification", "path": "/" }
            }
        },
        "paradym": {
            "base_url": "${VERIFIER_PARADYM_URL:-https://verifier.paradym.pensiondemo.findy.fi}",
            "credentials": {
                "pension_verification": { "type": "verification", "path": "/" }
            }
        }
    }
}
